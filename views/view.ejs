<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= post.title %></title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="blog-view">
      <div class="blog-hero">
        <% if (post.image) { %>
        <img src="<%= post.image %>" alt="blog image" class="blog-hero-img" />
        <% } %>
      </div>
      <div class="blog-content">
        <h1><%= post.title %></h1>
        <p class="author">By <%= post.author %></p>
        <p><%= post.content %></p>

        <!-- Buttons -->
        <div class="card-actions">
          <a href="/posts/<%= post._id %>/edit" class="card-btn edit"
            >✏️ Edit</a
          >
          <form
            action="/posts/<%= post._id %>?_method=DELETE"
            method="POST"
            style="display: inline"
          >
            <button type="submit" class="card-btn delete">🗑️ Delete</button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
